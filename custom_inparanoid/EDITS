./ was removed from the names of local scripts/tools, so this will not only work if those names are in the PATH
hard-coded full paths to some program components, won't work after moving the program...